# DragonForge Project

A programming language designed to aid programming language development by using metaprogramming.

## How?

By running compiled dragon-forge code on a binary interpeter, being called 'Anvil', it is possible to call the compiler through the interpreter.
In doing so, it is possible to generate, compile, integrate, run and get results back from code on the fly.

This allows for things like:
- Parser generators that are made and used at run time arbitrarily.
- Making the dragon-forge programming language able to be its own build scripting platform.
- Building complex pieces of code by script and not by hand.

# Hello World

Simple hello world code example:

```
[ entry point ]
df.main()() = {
    [ set string ]
    df.set("Roaring dragons sometimes slumber...%0a;")(message)

    [ print ]
    df.print.buffer_as_string(message)()
}
```

Compile and execute command:
```
./path/to/dragon-forge.elf ./relative/path/to/file.df
```

# Build

DragonForge is written in C for Linux.

It requires:
- gcc (or equivalent C compiler)
- xxd (for some internal code generation)

The executable files are always made one folder level above the repository folder.
It is recommended to make a 'dragon-forge-project' folder and clone / pull this repository inside of it.

Other than that, the only command you should need to execute to build the executable is this:

> make

# Art

![Logo (Generated by ChatGPT.)](./art/dragon-forge.png)

Roaring dragons sometimes slumber...
