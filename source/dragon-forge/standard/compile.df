[ default settings for compiler ]
df.compiler.configure.default_compile()(settings !df.compiler.settings) = {
    [ setup default settings ]
    df.copy(df.true)(settings:include_standard)
    df.copy(df.false)(settings:debug_enabled)
    df.copy(df.true)(settings:generate_kickstarter)
}
